import { LoginMeta, LoginPayload } from '../../interfaces';
import { createAsyncReducer } from '../../utils';

// import { loginActions } from '../actions';

// export default createAsyncReducer<LoginMeta, LoginPayload>(loginActions);
